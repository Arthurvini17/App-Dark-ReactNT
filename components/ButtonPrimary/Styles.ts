import { StyleSheet } from "react-native";

const Styles = StyleSheet.create({


    Button: {
        backgroundColor: '#fff',
        padding: 10,
        borderRadius: 8,
        width: 300,
    },

    title: {
        color: '#fff',
        textAlign: 'center'
    }
})

export default Styles;